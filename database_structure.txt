// GESTIONE ADMIN


tabella amministratore:

-ID_ADMIN || BIGINT || UNSIGNED || NOT_NULL || AUTO_INCREMENT
-NOME_ADMIN || VARCHAR(50) || NOT_NULL 
-COGNOME_ADMIN || VARCHAR(50) || NOT_NULL 
-EMAIL || VARCHAR(255) || NOT_NULL 
-PASSWORD || VARCHAR(255) || NOT_NULL

tabella caricamenti_admin:

-ID_CARICAMENTO || BIGINT || UNSIGNED || NOT_NULL || AUTO_INCREMENT
-NOME_CONTENUTO || VARCHAR(50) || NOT_NULL 
-CONTENUTO_CARICATO || VARCHAR(255) || NOT_NULL
-DATA_CARICAMENTO || CREATED_AT || UPTADED_AT || NULL
-CATEGORIE || VARCHAR(50) || NOT_NULL
-AUTORI || VARCHAR(50) || NOT_NULL
-ATTORI || VARCHAR(50) || NOT_NULL
-IN_ARRIVO || BOOLEAN || NOT_NULL
-DURATA || SMALLINT || NOT_NULL


// GESTIONE UTENTE

tabella utente:
-ID_UTENTE || BIGINT || UNSIGNED || NOT_NULL || AUTO_INCREMENT
-NOME || VARCHAR(50) || NOT_NULL 
-COGNOME | VARCHAR(50) || NOT_NULL 
-EMAIL || VARCHAR(255) || NOT_NULL 
-PASSWORD || VARCHAR(255) || NOT_NULL

- (per l'iscrizione inserire il metodo di pagamento usare braintree)
- (inserire tre piani di iscrizione basic,plus,premium,)

tabella abbonamento:

-ID_UTENTE
-PIANO_SCELTO // (per ogni piano stabilire una scadenza dell'abbonamento)
-PREZZO_MENSILE
-DATA_SCADENZA 

tabella autori:

-ID_AUTORE
-NOME
-COGNOME
-BIOGRAFIA
-FILM_PRODOTTI
-SERIE_PRODOTTI

tabella attori:

-ID_ATTORE
-NOME
-COGNOME
-BIOGRAFIA
-FILM_RECITATO
-SERIE_RECITATO

tabella categorie:

-ID_CATEGORIE
-NOME_CATEGORIA
-DESCRIZIONE


tabella visualizzazioni:

-ID_UTENTE
-CONTENUTO_VISUALIZZATO
-DATA VISUALIAZZIONE
-DISPOSITIVO_UTENTE

tabella feedback_utente:

-ID_UTENTE
-COMMENTO
-VOTO
-DATA_FEEDBACK


RELAZIONI TABELLE LINK FIGMA: https://www.figma.com/file/yen23yVKGm0kcNSOzQntDe/database-netflix-clone?type=whiteboard&node-id=0-1&t=2Yt5eBN4fvpMBQ8V-0